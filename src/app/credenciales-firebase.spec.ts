import { CredencialesFirebase } from './credenciales-firebase';

describe('CredencialesFirebase', () => {
  it('should create an instance', () => {
    expect(new CredencialesFirebase()).toBeTruthy();
  });
});
